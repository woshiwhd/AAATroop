# 资源迁移清单

## 已完成

- [x] 建立统一资源抽象层（`IAssetService` / `IAssetBackend` / 缓存 / 路由 / 策略）。
- [x] 建立三后端适配层（Resources、YooAsset、Addressables）。
- [x] 地图模板加载改为统一资源门面：
  - `ChunkTemplateLoader` -> `LoadTextAssetsByGroupAsync`
- [x] 地图 TileDatabase 寻址改造：
  - `TilemapManager` 支持 `tileDatabaseAssetKey` 通过 `AssetService` 加载
  - 移除 `TilemapManager` 中 `AssetDatabase` 与 `Resources.LoadAll` 的直连路径
- [x] 旧 `ResourcesProvider` 改为兼容桥接，不再直接 `Resources.Load`
- [x] 增加资源监控入口：`AssetDiagnostics.DumpMetrics()`

## 待配置（工程侧）

- [ ] 创建并填写 `AssetRoutingConfig` 资产。
- [ ] 创建并填写 `AssetGroupsConfig` 资产（特别是模板组 key 列表）。
- [ ] 在启动场景挂载 `AssetRuntimeBootstrap` 并绑定上述配置。
- [ ] 为 `TilemapManager.tileDatabaseAssetKey` 配置真实 key（如果不走场景直引）。

## 待验证

- [ ] 弱网下加载失败重试行为（观察 `AssetMetrics.retries/loadFailures`）。
- [ ] 后端不可用时的回退路径（Yoo/Addressables 未接入时应回退 Resources）。
- [ ] 场景切换与资源释放策略（确认无明显内存抖动）。

## 后续优化建议

- 接入真实 YooAsset 强类型 API，替换当前反射兼容逻辑。
- 对高频资源引入常驻组预热策略（`PreloadGroupAsync`）。
- 加入 CI 检查：无效 key、重复 key、未分组资源报警。

